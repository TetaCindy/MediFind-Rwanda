# For local development, leave this unset — the app defaults to
# http://localhost:5000/api automatically.
#
# When deploying (e.g. to Render as a Static Site), set this to your
# deployed backend's URL, for example:
#   REACT_APP_API_URL=https://medifind-rwanda-backend.onrender.com/api
REACT_APP_API_URL=
